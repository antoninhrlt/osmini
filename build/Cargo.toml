[package]
name = "core"
version = "0.1.0"
edition = "2021"

[dependencies]

[lib]
path = "../src/core/core.rs"
crate-type = ["staticlib"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
